#ifndef RTCD_SOLIDS_H
#define RTCD_SOLIDS_H

namespace rtcdNS {//real time collision detection

	/*class CollisionPlane;

	typedef Vector Point;*/

	//struct Plane {
	//	Plane(const CollisionPlane&);
	//	Vector n; // Plane normal. Points x on the plane satisfy Dot(n,x) = d
	//	float d; // d = dot(n,p) for a given point p on the plane
	//	Plane ComputePlane(Point a, Point b, Point c);
	//};
	//// Given three noncollinear points (ordered ccw), compute plane equation
	//


	//

	//// Region R = { (x, y, z) | (x-c.x)^2 + (y-c.y)^2 + (z-c.z)^2 <= r^2 }
	//struct Sphere {
	//	Point c; // Sphere center
	//	float r; // Sphere radius
	//};

}




#endif